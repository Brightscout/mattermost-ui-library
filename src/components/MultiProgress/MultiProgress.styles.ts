import styled from 'styled-components';

// Styles for MultiProgress bar container
export const StyledMultiProgressBarContainer = styled.div({
    '& .progress': {
        position: 'relative',
        height: '4px',
    },
});
