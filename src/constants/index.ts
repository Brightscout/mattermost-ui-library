import {CheckboxSizeTypes} from '@Components/CheckBox/CheckBox';

const CHECKBOX_SIZE: Record<string, CheckboxSizeTypes> = {
    SMALL: 'sm',
    MEDIUM: 'md',
    LARGE: 'lg',
};

export const Constants = {
    ITEM_HEIGHT: 37,
    FETCH_SUGGESTION_DELAY: 500,
    FETCH_FUNCTION_DELAY: 200,
    CHECKBOX_SIZE,
};
